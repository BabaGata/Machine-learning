function plotData(x, y)
figure; % open a new figure window

plot (x, y, 'rx', 'MarkerSize', 10);   % Plot the data
xlabel ('Population of city');         % Set the x-axis
ylabel ('Profit in 10,000$');          % set the x-axis


end
